<img src="../public/assets/burger.png" alt="A juicy hamburger">

<h1>Eat Da Burger!</h1>

<h3>Burgers Waiting to be Devoured:</h3>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
    {{> burgers/burger-block devoured=true}}
    {{/unless}}
    {{/each}}
</ul>

<h3>Burgers Already Devoured:</h3>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{> burgers/burger-block devoured=false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger</h2>
<form id="create-form">

    <div class="form-group">
        <label for="burger">Burger Name:</label>
        <input type="text" id="burger" name="burger">
    </div>

    <button type="submit">Add Burger</button>
</form>